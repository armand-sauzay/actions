name: CI

on:
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:      
        uses: ./lint

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: ./test